# Country Swing FSM

This project models country swing dance positions as states and dance moves as directed transitions between them.

The current version uses a hardcoded dataset and a small Qt GUI.

## Run

```bash
python main.py
```

## Install Dependencies

```bash
pip install -e .
```
